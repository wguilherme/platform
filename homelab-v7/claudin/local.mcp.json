{
  "mcpServers": {
    "claudin-discord": {
      "command": "bun",
      "args": ["run", "/Volumes/SSD/Projects/brainy/orion-pages/claudin/channels/discord-mcp.ts"],
      "env": {
        "DISCORD_WEBHOOK_URL": "${DISCORD_WEBHOOK_URL}",
        "HEALTHCHECK_PING_URL": "${HEALTHCHECK_PING_URL}",
        "MACHINE_NAME": "${MACHINE_NAME}"
      }
    }
  }
}
