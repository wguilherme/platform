apiVersion: tekton.dev/v1
kind: TaskRun
metadata:
  name: bun-hello-bootstrap
  namespace: tekton-pipelines
spec:
  taskRef:
    name: bun-hello-ci
  workspaces:
    - name: source
      emptyDir: {}
