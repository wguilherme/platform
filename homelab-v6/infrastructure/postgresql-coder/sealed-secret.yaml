# ATENÇÃO: Este arquivo precisa ser gerado com kubeseal.
#
# Passos para gerar:
#
# 1. Gere uma senha segura para o banco:
#    DB_PASSWORD=$(openssl rand -base64 24)
#    echo "Senha gerada: $DB_PASSWORD"
#
# 2. Crie o Secret original (não commitar):
#    kubectl create secret generic postgresql-coder-credentials \
#      --namespace postgresql-coder \
#      --from-literal=postgres-password="$DB_PASSWORD" \
#      --from-literal=password="$DB_PASSWORD" \
#      --dry-run=client -o yaml > /tmp/postgresql-coder-secret.yaml
#
# 3. Seal com kubeseal:
#    kubeseal --format yaml \
#      --controller-name sealed-secrets-controller \
#      --controller-namespace kube-system \
#      < /tmp/postgresql-coder-secret.yaml \
#      > homelab-v6/infrastructure/postgresql-coder/sealed-secret.yaml
#
# 4. Salvar DB_PASSWORD para uso no coder/sealed-secret.yaml:
#    A connection string será:
#    postgres://coder:<DB_PASSWORD>@postgresql-coder-postgresql.postgresql-coder.svc.cluster.local:5432/coder?sslmode=disable
#
# Substitua este arquivo pelo output do kubeseal antes de fazer commit.
