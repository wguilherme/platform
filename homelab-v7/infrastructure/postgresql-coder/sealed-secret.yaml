# ATENÇÃO: Substituir pelo output do kubeseal antes de commitar com valor real.
#
# 1. Gere a senha:
#    DB_PASSWORD=$(openssl rand -base64 24)
#
# 2. Crie e seal:
#    kubectl create secret generic postgresql-coder-credentials \
#      --namespace postgresql-coder \
#      --from-literal=postgres-password="$DB_PASSWORD" \
#      --from-literal=password="$DB_PASSWORD" \
#      --dry-run=client -o yaml \
#    | kubeseal --format yaml --cert sealed-secrets-cert.pem \
#    > homelab-v7/infrastructure/postgresql-coder/sealed-secret.yaml
#
# Connection string para o coder:
#   postgres://coder:<DB_PASSWORD>@postgresql-coder-postgresql.postgresql-coder.svc.cluster.local:5432/coder?sslmode=disable
