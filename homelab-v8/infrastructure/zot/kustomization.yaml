apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - helmrelease.yaml
namespace: zot
generatorOptions:
  disableNameSuffixHash: true
configMapGenerator:
  - name: zot-values
    namespace: zot
    files:
      - values.yaml=values.yaml
