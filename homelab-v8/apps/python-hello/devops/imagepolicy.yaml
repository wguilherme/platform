apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: python-hello
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: python-hello
  policy:
    alphabetical:
      order: asc
  filterTags:
    pattern: '^\d{14}-[a-f0-9]{8}$'
