# ATENÇÃO: Substituir pelo output do kubeseal antes de commitar com valor real.
#
# ── 1. DB URL ────────────────────────────────────────────────────────────────
#    kubectl create secret generic coder-db-url --namespace coder \
#      --from-literal=url="postgres://coder:<DB_PASSWORD>@postgresql-coder-postgresql.postgresql-coder.svc.cluster.local:5432/coder?sslmode=disable" \
#      --dry-run=client -o yaml \
#    | kubeseal --format yaml --cert sealed-secrets-cert.pem >> sealed-secret.yaml
#
# ── 2. Claude OAuth Token ────────────────────────────────────────────────────
#    claude setup-token   # roda localmente, copia sk-ant-oat01-...
#    kubectl create secret generic claude-oauth --namespace coder \
#      --from-literal=token="sk-ant-oat01-XXXXXXXXXX" \
#      --dry-run=client -o yaml \
#    | kubeseal --format yaml --cert sealed-secrets-cert.pem >> sealed-secret.yaml
#
# ── 3. GitHub OAuth App ──────────────────────────────────────────────────────
#    Criar em: github.com/settings/developers → OAuth Apps
#    Callback URL: https://coder.wguilherme.com/api/v2/users/oauth2/github/callback
#    kubectl create secret generic coder-github-oauth --namespace coder \
#      --from-literal=client-id="<ID>" --from-literal=client-secret="<SECRET>" \
#      --dry-run=client -o yaml \
#    | kubeseal --format yaml --cert sealed-secrets-cert.pem >> sealed-secret.yaml
#
# ── 4. Jira OAuth App ────────────────────────────────────────────────────────
#    Criar em: developer.atlassian.com → Create app → OAuth 2.0 (3LO)
#    Callback URL: https://coder.wguilherme.com/api/v2/external-auth/jira/callback
#    kubectl create secret generic coder-jira-oauth --namespace coder \
#      --from-literal=client-id="<ID>" --from-literal=client-secret="<SECRET>" \
#      --dry-run=client -o yaml \
#    | kubeseal --format yaml --cert sealed-secrets-cert.pem >> sealed-secret.yaml
