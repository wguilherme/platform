apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: bun-hello
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: bun-hello
  policy:
    alphabetical:
      order: asc
  filterTags:
    pattern: '^\d{14}-[a-f0-9]{8}$'
