---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  # tekton install: gerenciado via 'make tekton-install', não pelo Flux
  # config/: adicionar após Tekton CRDs instalados via make tekton-install
