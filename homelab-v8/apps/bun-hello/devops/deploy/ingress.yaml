apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bun-hello
  namespace: default
spec:
  ingressClassName: nginx
  rules:
    - host: bun.wguilherme.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: bun-hello
                port:
                  number: 80
